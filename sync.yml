trigger:
  branches:
    include:
    - main

resources:
  repositories:
    - repository: templates
      type: git
      name: Plumbing/core-templates
    - repository: policiesy
      type: git
      name: Plumbing/policies

stages:
- template: stages/sync/azure-sync.yml@templates  # Template reference
  parameters:
    checkout: policiesy
